{
  "type": "sqlite",
  "database": "database.sqlite",
  "synchronize": true,
  "logging": false,
  "entities": ["out/entity/**/*.ts", "out/entity/**/*.js"],
  "migrations": ["out/migration/**/*.ts"],
  "subscribers": ["out/subscriber/**/*.ts"],
  "cli": {
    "entitiesDir": "src/entity",
    "migrationsDir": "src/migration",
    "subscribersDir": "src/subscriber"
  }
}
